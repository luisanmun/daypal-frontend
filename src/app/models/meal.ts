export interface Meal {
    title?: string;
    description?: string;
    calories?: number;
    category?: string;
    id?: number;

    // constructor(title: string,description: string,calories: number,category: string) {
    // }
}